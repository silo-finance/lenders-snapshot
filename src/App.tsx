export default function App() {
  return (
    <main className="min-h-screen bg-slate-950 text-white">
      <section className="mx-auto flex min-h-screen max-w-6xl flex-col justify-center px-6 py-16">
        <p className="text-sm font-semibold uppercase tracking-[0.35em] text-emerald-300">
          Lenders Snapshot
        </p>
        <h1 className="mt-4 max-w-3xl text-5xl font-semibold tracking-tight text-white">
          Browse static Silo lender snapshots.
        </h1>
        <p className="mt-5 max-w-2xl text-lg leading-8 text-slate-300">
          The initial app shell is ready. Snapshot data, tables, and reward distribution tooling will be
          layered onto this static Vite build.
        </p>
      </section>
    </main>
  );
}
