# Copy to .env and fill in. The real .env is gitignored and must never be committed.

# Archive RPC endpoint for Sonic (must support eth_call at the historical block).
RPC_URL=

# The Graph gateway Bearer token.
THE_GRAPH_API_KEY=
